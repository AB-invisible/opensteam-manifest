const APP_NAME = process.env.BRAND_NAME || 'OpenSteam';
const APP_TAGLINE = process.env.BRAND_TAGLINE || 'OpenSteam Manifests';
const NETWORK_NAME = `${APP_NAME} Network`;

module.exports = {
  APP_NAME,
  APP_TAGLINE,
  NETWORK_NAME,
};
