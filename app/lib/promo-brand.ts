/** PDF + web chrome copy for promotional rank exams (distinct from the trial assessment). */
export const PROMO_BRAND = {
  primaryLine: "OpenSteam | Manifests",
  handbookLine: "Moderator Promotion Exam",
  assessmentLine: "Rank promotion assessment (timed examination)",
} as const;

export type PromoBrand = typeof PROMO_BRAND;
